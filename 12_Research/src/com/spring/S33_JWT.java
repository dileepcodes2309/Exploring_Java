package com.spring;

public class S33_JWT {

    public static void main(String[] args) {
        String jwt = """
                -> JWT - JSON Web Tokens
                -> Instead of sending the username and password every time,
                   the server gives the client a token after a successful login.
                -> This token is a signed string containing encoded information about
                   the user (like their username and roles).
                """;
    }
}
