package com.spring;

public class S06_DependencyInjection {
    public static void main(String[] args) {

        String dependencyInjection = """
                -> It is an design pattern which says the objects don't need to create their  own
                   dependencies instead container create the required dependencies.
                -> It makes them loosely coupled, easier to maintain and easier to Unit test.
                """;
    }
}
