package com.spring;

public class S13_Autowired {
    public static void main(String[] args) {

        String autoWired = """
                -> The @Autowired annotation in Spring is a mechanism for automatic dependency injection.
                -> It allows the Spring IoC (Inversion of Control) container to automatically resolve and inject dependencies into the Spring-managed components (beans)
                   without requiring explicit configuration in XML or Java.
                """;
    }
}
