package com.spring;

public class S09_ApplicationProperties {
    public static void main(String[] args) {

        String applicationProperties = """
                -> We store all our application configuration in application.properties.
                -> Instead of hardcoding values, we put them there.
                -> So they can be easily changed without changing the code.
                """;
    }
}
