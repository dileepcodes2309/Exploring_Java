package com.spring;

public class S19_Spring_Bean {
    public static void main(String[] args) {

        String springBean = """
                -> A java class which is managed by the IOC container is called as Spring Bean.
                -> The life cycle of the spring bean is taken care by the IOC container.
                -> Spring beans are instantiated, configured, wired, and managed by IoC container.
                -> Beans are created with the configuration metadata that the users supply to the container
                   (by means of XML or java annotations configurations.)
                """;
    }
}
