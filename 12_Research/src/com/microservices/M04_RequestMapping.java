package com.microservices;

public class M04_RequestMapping {
    public static void main(String[] args) {

        String role = """
                -> @RequestMapping is used to map HTTP requests (URLs + HTTP methods) to
                   specific controller methods.
                -> It tells Spring which method should handle which request.
                """;
    }
}
