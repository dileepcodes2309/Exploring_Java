package com.microservices;

public class M11_Docker_Compose_File {
    public static void main(String[] args) {

        String whatIsIt = """
                A Docker Compose file (docker-compose.yml) is a YAML configuration file
                used to define and run multiple Docker containers together.
                """;
    }
}
