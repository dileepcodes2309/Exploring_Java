package com.angular;

public class A01_SinglePageApplication {
    public static void main(String[] args) {

        String singlePageApplication = """
                -> In Single Page Application, the page loads only once and after that its contents
                   loads dynamically.
                -> Example: Gmail-> Click email → page does NOT refresh
                """;
    }
}
